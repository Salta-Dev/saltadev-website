"""Benefits app configuration."""

from django.apps import AppConfig


class BenefitsConfig(AppConfig):
    """Configuration for the benefits app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "benefits"
    verbose_name = "Beneficios"
